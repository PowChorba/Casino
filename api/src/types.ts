
export interface UsersFormat { 
    nickname: string
    email: string
    password: string
}

export interface GameFormat {
  _id: string;
  title: string;
  proveedor: string;
  img: string;
  iframe: string;
}